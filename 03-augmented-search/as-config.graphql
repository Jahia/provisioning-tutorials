mutation {
    admin {
        search {
            setDbConnection(connectionId: "augmented-search-conn")
            addSite(siteKey: "digitall")
            startIndex(siteKeys: []) {
                 jobs {
                     id
                 }
             }
        }
    }
}
